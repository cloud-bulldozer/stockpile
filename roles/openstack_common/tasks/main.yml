---
- name: Check for container cli
  command: hiera -c /etc/puppet/hiera.yaml container_cli
  become: true
  ignore_errors: true
  register: container_cli

- name: Set container cli
  set_fact:
    container_cli: "{{ (container_cli == 'nil') | ternary('docker', 'podman') }}"

- name: Determine if container is running
  shell: "{{ container_cli }} ps | wc -l"
  register: container_ps

- name: Set var for container deployment
  set_fact:
    containers: True
    config_path: /var/lib/config-data/puppet-generated
  when: container_ps.stdout|int > 1

- name: Set fact for non-container deployment
  set_fact:
    config_path: /etc
  when: container_ps.stdout|int < 2

