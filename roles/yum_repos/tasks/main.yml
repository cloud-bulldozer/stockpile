---
- name: check if yum is present
  command: python -c "import yum"
  register: yum_installed
  ignore_errors: yes
  changed_when: false

- block:
    - name: collect yum repos
      yum:
        list: repos
      register: yum_repos

    - name: Storing the results
      set_fact:
        stockpile_yum_repos: "{{ yum_repos.results }}"
  when: yum_installed.rc == 0
